# AGENTS.md

## 适用范围

本文件适用于当前目录及其子目录。这里维护的是一个零依赖、可直接在现代桌面浏览器中运行的 Canvas 小游戏“鼠标躲陨石”。未来将本目录迁移为独立项目时，本文件继续作为代理协作和验收规范。

## 当前项目形态

- 权威版本是 `鼠标躲陨石游戏.html`。
- `鼠标躲陨石游戏_v2.html`、`鼠标躲陨石游戏_v3.html`、`鼠标躲陨石游戏_v4.html` 是历史快照，默认只读。除非任务明确要求，不要修补、同步或删除这些文件。
- 当前实现将 HTML、CSS 和 JavaScript 放在一个文件中，不依赖构建工具、包管理器、框架、远程脚本或外部素材。
- 游戏应能通过浏览器直接打开运行。使用本地 HTTP 服务进行测试可以接受，但不能因此把服务端或构建步骤变成运行前提。

## 工作原则

1. 先阅读当前权威文件和与任务相关的已有说明，再修改代码；不要根据历史版本推断现状。
2. 采用满足需求的最小变更，避免顺手重构、整体格式化、重命名或改写无关视觉效果。
3. 保持单文件和零依赖形态，除非用户明确批准项目拆分或引入依赖。
4. 不要为了测试而在正式代码中暴露调试接口。浏览器探针应在测试会话中临时注入。
5. 不要声称“已修复”或“验证通过”，除非实际运行了与变更风险相匹配的检查。
6. 保留用户已有文件和无关改动。若目录已成为 Git 仓库，只暂存和提交本次任务涉及的文件。

## 关键行为约束

修改游戏逻辑时，除非需求明确要求改变，必须保留以下行为：

- 鼠标移动控制飞船；按住左键进入瞄准状态，松开后在能量足够时跃迁到目标点并消耗 50 点能量。
- 瞄准期间只有陨石的 `x`、`y` 位移降为正常速度的 70%。旋转、碰撞检测、能量恢复、特效时序和其他系统仍按正常时间运行。
- 玩家初始生命为 100，经验晶体恢复 5 点生命并增加 100 分；每收集 5 个晶体刷新一轮。
- 难度随分数提高，活跃陨石目标数量仍受 80 个上限约束。
- `requestAnimationFrame` 主循环和全局事件监听只注册一次。重新开始游戏应重置状态，不得叠加循环或监听器。
- 每个音效创建的 `OscillatorNode` 和 `GainNode` 在播放结束后都要主动 `disconnect()`，避免同一浏览器会话中节点持续累积。

## 排行榜与隐私

- 保持现有存储键不变：`meteorMouseTop3` 和 `meteorMouseLastPlayer`。
- 排行榜按玩家原始姓名合并，只保留同名最高分，降序取前三名。
- 姓名脱敏只发生在 DOM 展示层；`localStorage` 中必须保存原始姓名，否则会破坏同名最高分逻辑。
- 脱敏按 Unicode 字符处理，使用 `Array.from()` 或等效的字符安全方式：
  - 1 个字符：原样显示，例如 `王`。
  - 2 个字符：保留首字符，第二个字符显示为 `*`，例如 `张三` → `张*`。
  - 3 个及以上字符：保留首尾，中间每个字符显示为一个 `*`，例如 `王小明` → `王*明`、`欧阳小明` → `欧**明`。
- 不要清空、迁移或改写玩家已有的本地排行榜数据，除非任务明确要求。

## 性能与资源生命周期

- 优先寻找可证明的累积点，不要仅凭体感把卡顿归因于 Canvas 绘制。
- 检查动画循环、事件监听、Web Audio 节点和粒子、光圈、浮动文字、尾焰等数组的生命周期。
- 新增的临时对象必须能在结束后被移除，或有明确上限。
- 不要在每帧创建无界增长的集合、重复监听器或新的动画循环。
- 音频回归测试要使用刷新后的页面或新的隔离浏览器会话，避免之前触发的音效污染节点计数。

## 修改后的最低验证

根据改动范围执行相关项目；涉及核心循环、输入或状态时，应覆盖全部项目。

### 静态检查

- 确认 HTML 内的 JavaScript 可解析，没有残留调试代码、占位符或意外的外部依赖。
- 检查最终差异，只包含任务所需修改，历史版本未被意外改动。

### 浏览器功能回归

- 页面能正常打开，控制台没有新的错误。
- 玩家姓名校验、开始游戏、游戏结束、返回登记和再次开始均可用。
- 鼠标跟随、按住左键瞄准、松开跃迁、能量消耗与恢复正常。
- 瞄准时陨石位移为正常状态的 70%，松开或取消瞄准后立即恢复。
- 陨石碰撞、短暂无敌、经验拾取、生命与分数变化、经验波次刷新正常。
- 难度提升、陨石补充和数量上限正常。
- 排行榜 Top 3、同名最高分、刷新后的持久化和展示脱敏同时正确。
- 连续重开不会增加主循环或事件监听，临时效果数组会被重置或回收。

### 音频与长会话回归

- 音频必须由用户交互解锁，不得引入自动播放错误。
- 连续触发至少 20 次目标音效，确认每个振荡器和增益节点在结束后都被断开。
- 最终控制台错误检查应使用干净会话，避免把故意制造的失败或旧日志算作当前回归。

## 测试记录与交付说明

- 报告实际执行过的检查及结果；未执行的项目要明确说明，不能用“应该没问题”代替。
- 浏览器探针得到的计数必须说明起始条件、触发次数和是否使用隔离会话。
- 若发现需求与上述既有行为冲突，先指出冲突及影响，再等待用户确认采用哪一条规则。
- 除非用户明确要求，不创建新版本副本，不提交 Git，不发布或部署游戏。

## 独立项目迁移约定

将本目录迁移为独立项目时，优先保持 `鼠标躲陨石游戏.html` 可直接运行。可以按实际需要补充 `README.md`、自动化测试或开发服务器配置，但不要在没有明确收益和批准的情况下引入框架或拆散单文件实现。若以后决定模块化，应先记录新的入口文件、运行命令、数据兼容策略和完整回归命令，再更新本文件。
